// app/handheld/src/pako.min.js
//
// 注意：此文件**不是** pako 库的实际拷贝。我们改用浏览器原生
// `CompressionStream` API（Android WebView 80+ 支持），见 `gzip.js`。
// 此文件保留是为了占位与文档：如果将来需要 vendor pako@2.1.0，可以从
// https://cdn.jsdelivr.net/npm/pako@2.1.0/dist/pako.min.js 下载并放在这里。
//
// 当前实现下，`upload-usb.js` 不会 import 此文件；它直接使用 `gzip.js`。
//
// TODO: vendor pako@2.1.0 from npm if CompressionStream proves unreliable on
// older Android WebView builds.

/* eslint-disable */
"use strict";
// no-op stub
window.pako = window.pako || null;
